name: invest_dwh
version: '1.0.0'
config-version: 2

# Name of the block in ~/.dbt/profiles.yml
profile: invest_dwh

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  invest_dwh:
    staging:
      +materialized: view
